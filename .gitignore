node_modules/
*.log

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Anything unpacked or zipped for distribution
*.zip
*.crx
*.pem

# Local tool artifacts. bmfinder.db holds saved people and observed rosters,
# which is exactly the data this tool exists to keep private.
__pycache__/
*.pyc
local/bmfinder.db
*.sqlite
*.sqlite3

# Scratch output from the Dieter Rams design audits. The findings that mattered
# were acted on in the code; the artifacts are working notes, not deliverables.
DESIGN-IS-*/

# Unsent draft correspondence, still carrying [PLACEHOLDER] tokens.
docs/BM Enquiry.txt

# Build artifact. Regenerate with "npm run package" before uploading to the
# Chrome Web Store; it is byte-verified against extension/, so committing it
# would only duplicate every source change in the diff.
webstore/
